apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-b
  labels:
    app: app-b
spec:
  replicas: 2
  selector:
    matchLabels:
      app: app-b
  template:
    metadata:
      labels:
        app: app-b
    spec:
      containers:
      - name: app-b-server
        image: sso-platform/app-b:latest
        ports:
        - containerPort: 3002
        env:
        - name: NODE_ENV
          value: production
        - name: PINGONE_CLIENT_ID
          valueFrom:
            secretKeyRef:
              name: pingone-credentials
              key: client-id
        - name: PINGONE_CLIENT_SECRET
          valueFrom:
            secretKeyRef:
              name: pingone-credentials
              key: client-secret
        - name: PINGONE_ENVIRONMENT_ID
          valueFrom:
            configMapKeyRef:
              name: pingone-config
              key: environment-id
        livenessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: app-b-service
spec:
  selector:
    app: app-b
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3002
  type: LoadBalancer
